// Editor font size for a query object's CodeMirror editor. Named (not a bare
// literal) so the font-discipline scan does not flag a numeric `fontSize`.
const SQL_FONT_SIZE = 12;

export { SQL_FONT_SIZE };
