// File-system markers that identify a SQLMesh project root. Domain-owned single
// source of truth, consumed by the project store and the left rail.
const SQLMESH_PROJECT_MARKERS = ["config.yaml", "config.yml"];

export { SQLMESH_PROJECT_MARKERS };
