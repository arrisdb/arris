const DEFAULT_BRANCH_NAME = "No branch";
const DEFAULT_PROJECT_NAME = "No Project";
const MAIN_WORKTREE_NAME = "main worktree";
const MAIN_WORKTREE_LABEL = "main";
const REMOTE_BRANCH_PREFIX = "origin/";
const TOP_BAR_TABS = {
  branches: "branches",
  stash: "stash",
} as const;

export {
  DEFAULT_BRANCH_NAME,
  DEFAULT_PROJECT_NAME,
  MAIN_WORKTREE_LABEL,
  MAIN_WORKTREE_NAME,
  REMOTE_BRANCH_PREFIX,
  TOP_BAR_TABS,
};
