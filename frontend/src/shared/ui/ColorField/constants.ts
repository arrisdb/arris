/// Popover geometry. The width matches the react-colorful surface plus the
/// popover's own padding, so the panel hugs the picker.
const POPOVER_WIDTH = 196;
const POPOVER_GAP = 6;
const VIEWPORT_PAD = 8;

export { POPOVER_GAP, POPOVER_WIDTH, VIEWPORT_PAD };
