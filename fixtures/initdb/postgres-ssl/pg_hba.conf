# HBA for the SSL-enabled Postgres test instance (arris-pg-ssl).
# certuser authenticates by client certificate (mTLS); everyone else by password.
local     all all                trust
hostssl   all certuser   all     cert
host      all all        all     scram-sha-256
